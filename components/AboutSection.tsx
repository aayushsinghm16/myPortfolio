'use client';
import React from 'react';

// AboutSection is intentionally empty — the Stitch design does not include
// an "About" bento-grid on the home page. The standalone /about route still
// renders its own page component.
const AboutSection: React.FC = () => {
  return null;
};

export default AboutSection;
